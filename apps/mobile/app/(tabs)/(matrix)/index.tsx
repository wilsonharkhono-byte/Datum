import { Screen } from "@/components/ui/Screen";
import { Text } from "@/components/ui/Text";
// Placeholder — replaced by Task 9 (Matrix projects screen on @datum/core).
export default function MatrixScreen() {
  return <Screen><Text variant="heading">Matrix</Text><Text variant="muted">Slice: projects-board</Text></Screen>;
}
