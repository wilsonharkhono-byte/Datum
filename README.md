# DATUM

Internal coordination hub for WHAstudio finishing-phase projects. Slice 0 = Foundation.

## Setup
See `docs/setup.md` (filled in by Task 18 of the Slice 0 plan).

## Apps
- `apps/web` — Next.js admin (port 3000)
- `apps/mobile` — Expo React Native

## Packages
- `packages/db` — Supabase migrations, seed, generated types
- `packages/types` — shared domain types
